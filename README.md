# Scalpello

The universal scraper

# TODO features

* nested scraping
* upsert on DB
* HTTP interface
* Docker container
* fields mapping
* scraping from HTML
* multi scraping
* allow updates on a primary key